<%= form_for(@authenticate) do |f| %>
  <% if @authenticate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@authenticate.errors.count, "error") %> prohibited this authenticate from being saved:</h2>

      <ul>
      <% @authenticate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :password %><br>
    <%= f.text_field :password %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
