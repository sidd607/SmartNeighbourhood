<div class="questionnaires_<%= action_name %>">
  <h1>
      Surveys Index
  </h1>
  <hr>
  <%= link_to "New Survey", new_survey %>

  <ul>
    <% @surveys.each do |survey| %>
      <li>
        <%= link_to survey.name, new_attempt(survey.id) , :id => survey.id %>
         <%= link_to "Response stats" , :controller => 'surveys' , :action => 'show', :id => survey.id  %> 
      </li>
    <% end -%>
  </ul>

</div>
